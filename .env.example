# Neon Postgres connection string.
# On Vercel, this is auto-injected when you attach a Postgres DB from the dashboard.
# For local dev with `vercel dev`, copy to .env.local and paste your Neon URL.
DATABASE_URL=postgres://user:password@host.neon.tech/dbname?sslmode=require
